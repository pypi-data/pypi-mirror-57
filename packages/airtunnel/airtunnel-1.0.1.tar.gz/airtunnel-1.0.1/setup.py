#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['airtunnel',
 'airtunnel.metadata',
 'airtunnel.operators',
 'airtunnel.operators.sql',
 'airtunnel.sensors']

package_data = \
{'': ['*']}

install_requires = \
['pandas >= 0.23', 'schema >= 0.6', 'apache-airflow >= 1.10', 'pyarrow >= 0.13']

extras_require = \
{'docs': ['sphinx >= 2.0.0, < 3', 'sphinx_rtd_theme'],
 'pyspark': ['pyspark >= 2.3'],
 'testing': ['pytest-cov', 'pytest', 'xlrd', 'flake8', 'xlwt']}

setup(name='airtunnel',
      version='1.0.1',
      description='airtunnel – tame your Airflow!',
      author='Jörg Schneider',
      author_email='joerg@joerg-schneider.org',
      url='https://github.com/joerg-schneider/airtunnel',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.6',
     )
