try:
    # pylint: disable=wildcard-import, unused-wildcard-import
    from mypy_boto3_networkmanager.paginator import *
except ImportError:
    raise ImportError(
        "Install boto3-stubs[networkmanager] to use networkmanager Paginator annotations"
    )
