from setuptools import setup

setup(name='hendria_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['hendria_distributions'],
      zip_safe=False)
