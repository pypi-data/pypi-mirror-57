from setuptools import setup

setup(name='distributions-adarbha',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions-adarbha'],
      zip_safe=False)
