from setuptools import setup

setup(name='distributions_adarbha',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['distributions_adarbha'],
      zip_safe=False)
